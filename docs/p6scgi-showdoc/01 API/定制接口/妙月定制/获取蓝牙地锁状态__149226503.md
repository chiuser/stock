<!--
source: P6SCGI ShowDoc
item_name: P6SCGI API（CN）
item_id: 663863654
page_id: 149226503
source_url: http://p6scgi-api.p6sai.com:4999/web/#/663863654/149226503
catalog_path: 01 API / 定制接口 / 妙月定制
-->

# 获取蓝牙地锁状态
##### 简要描述

- ##### 请求

**xml**

```
<GroundLockStatus>
    <GroundLockID><!-- rw,opt,xs:string,地锁 ID, the max length : 32--></GroundLockID>
</GroundLockStatus>
```

**字段说明**

| 字段 | 类型 | 描述 | 是否必须 | 是否有效 | 是否可为空 | 可读可写(rw)/只读(ro)/只写(wo) | 取值范围/长度 | 备注 | 在APP的哪一个页面的哪一个字段 | 在web的哪一个页面的哪一个字段 | 字段受哪一个能力限制 | 字段受哪个字段限制 |
| --- | --- | --- | ---- | ---- | ----- | ---------------------- | ------- | --- | ---------------- | ---------------- | ---------- | --------- |
| GroundLockID | string | <span class="colour" style="color:rgb(106, 153, 85)">地锁 ID</span> | 否 | 是 | 否 | 可读可写 | 最大长度32 |  |  | <span class="colour" style="color: rgb(206, 145, 120);">配置-智能分析-车牌抓拍-车位设置(蓝牙地锁)-左车位配置/右车位配置-地锁ID</span> |  |  ||

##### 响应

**xml**

```
<GroundLockStatus>
    <GroundLockID><!-- rw,opt,xs:string,地锁 ID, the max length : 32--></GroundLockID>
    <GroundLockStatus><!-- ro,opt,xs:integer,地锁状态， 0 - 落下，1 - 升起--></GroundLockStatus>
</GroundLockStatus>
```

**字段说明**

| 字段 | 类型 | 描述 | 是否必须 | 是否有效 | 是否可为空 | 可读可写(rw)/只读(ro)/只写(wo) | 取值范围/长度 | 备注 | 在APP的哪一个页面的哪一个字段 | 在web的哪一个页面的哪一个字段 | 字段受哪一个能力限制 | 字段受哪个字段限制 |
| --- | --- | --- | ---- | ---- | ----- | ---------------------- | ------- | --- | ---------------- | ---------------- | ---------- | --------- |
| GroundLockID | string | <span class="colour" style="color:rgb(106, 153, 85)">地锁 ID</span> | 否 | 是 | 否 | 可读可写 | 最大长度32 |  |  | <span class="colour" style="color: rgb(206, 145, 120);">配置-智能分析-车牌抓拍-车位设置(蓝牙地锁)-地锁连接-地锁ID</span> |  |  |
| GroundLockStatus | integer | <span class="colour" style="color:rgb(106, 153, 85)">地锁状态，0 - 落下； 1 - 升起</span> | 否 | 是 | 否 | 只读 | 0/1 |  |  | <span class="colour" style="color: rgb(206, 145, 120);">配置-智能分析-车牌抓拍-车位设置(蓝牙地锁)-地锁连接-地锁状态</span> |  |  ||

##### 请求URL

 - `/System/GroundLockStatus` 

##### 请求方式

- post
 
##### 成功返回示例  
```
[]

```

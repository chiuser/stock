<!--
source: P6SCGI ShowDoc
item_name: P6SCGI API（CN）
item_id: 663863654
page_id: 149227075
source_url: http://p6scgi-api.p6sai.com:4999/web/#/663863654/149227075
catalog_path: 01 API / 面板机功能接口 / 体温检测配置管理
-->

# 获取体温异常报警配置
##### 简要描述

- ##### 请求

**xml**
无
**字段说明**
无

##### 响应

**xml**

```
<FaceTemperatureAbnormalAlarmConfig>
    <ChannelID><!--ro,req,xs:integer--></ChannnelID>
    <VoiceAlert><!--rw,req,xs:string”Enable”,”Disable”--></VoiceAlert>
    <DisableWhiteListLinkage><!--rw,req,xs:string”Enable”,”Disable”--></DisableWhiteListLinkage>
    <PushToPhone><!--rw,req,xs:string”Enable”,”Disable”--></PushToPhone>
    <AlarmOut1><!--rw,req,xs:string”Enable”,”Disable”--></AlarmOut1>
    <AlarmOut2><!--rw,req,xs:string”Enable”,”Disable”--></AlarmOut2>
    <Email><!--rw,req,xs:string”Enable”,”Disable”--></Email>
</FaceTemperatureAbnormalAlarmConfig>
```

**字段说明**

| 字段 | 类型 | 描述 | 是否必须 | 是否有效 | 是否可为空 | 可读可写(rw)/只读(ro)/只写(wo) | 取值范围/长度 | 备注 |
| --- | --- | --- | ---- | ---- | ----- | ---------------------- | ------- | --- |
| ChannelID | integer | 通道号，对于IPC来说值为1 | 是 | 是 | 否 | 只读 | [1-32] |  |
| VoiceAlert | string | 是否使能语音报警；Disable 去使能，Enable 使能 | 是 | 是 | 否 | 可读可写 | 动态数组 |  |
| DisableWhiteListLinkage | string | 是否使能禁止白名单联动项；Disable 去使能，Enable 使能 | 是 | 是 | 否 | 可读可写 | 动态数组 |  |
| PushToPhone | string | 是否使能手机推送；Disable 去使能，Enable 使能 | 是 | 是 | 否 | 可读可写 | 动态数组 |  |
| AlarmOut1 | string | 是否使能报警输出1；Disable 去使能，Enable 使能 | 是 | 是 | 否 | 可读可写 | 动态数组 |  |
| AlarmOut2 | string | 是否使能报警输出2；Disable 去使能，Enable 使能 | 是 | 是 | 否 | 可读可写 | 动态数组 |  |
| Email | string | 是否使能邮件推送；Disable 去使能，Enable 使能 | 是 | 是 | 否 | 可读可写 | 动态数组 |  | |

##### 请求URL

 - `/FaceTemperatureDetect/ChannelID/FaceTemperatureAbnormalAlarmConfig` 

##### 请求方式

- get
 
##### 成功返回示例  
```
[]

```

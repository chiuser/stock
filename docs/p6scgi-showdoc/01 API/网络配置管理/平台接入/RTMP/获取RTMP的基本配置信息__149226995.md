<!--
source: P6SCGI ShowDoc
item_name: P6SCGI API（CN）
item_id: 663863654
page_id: 149226995
source_url: http://p6scgi-api.p6sai.com:4999/web/#/663863654/149226995
catalog_path: 01 API / 网络配置管理 / 平台接入 / RTMP
-->

# 获取RTMP的基本配置信息
##### 简要描述

- ##### 说明

如果<IsSupportRetrievePushAddressFromServer>的值为“trun”，表示设备支持从特定服务器检索推送流URL。 当“假”时，推送流URL必须是手动特定的。
<模式>“动态”或“手动”。
<PushURLGeneratorServer>是服务器URL，此服务器必须由我们的协议规定。 如果你想建立一个服务器，你应该联系我们。

##### 请求

**xml**
无
**字段说明**
无

##### 响应

**xml**

```
<?xml version="1.0" encoding="utf-8"?>
<BasicConfig>
    <IsSupportRetrievePushAddressFromServer><!--ro,opt,xs:boolean;"false","true"--></IsSupportRetrievePushAddressFromServer>
    <Mode><!--rw,opt,xs:string, "dynamic, manual"--></Mode>
    <PushURLGeneratorServer><!--rw,opt,xs:string, the max length : 128--></PushURLGeneratorServer>
</BasicConfig>
```

**字段说明**

| 字段 | 类型 | 描述 | 是否必须 | 是否有效 | 是否可为空 | 可读可写(rw)/只读(ro)/只写(wo) | 取值范围/长度 | 备注 |
| --- | --- | --- | ---- | ---- | ----- | ---------------------- | ------- | --- |
| IsSupportRetrievePushAddressFromServer | boolean | <span class="colour" style="color:rgb(106, 153, 85)">是否支持从服务器获取推流地址；false 否，true 是</span> | 否 | 是 | 否 | 只读 | false/true |  |
| Mode | string | <span class="colour" style="color:rgb(106, 153, 85)">当前从服务器获取推流地址模式；</span><span class="colour" style="color:rgb(206, 145, 120)">dynamic </span><span class="colour" style="color:rgb(106, 153, 85)">自动，</span><span class="colour" style="color:rgb(206, 145, 120)">manual </span><span class="colour" style="color:rgb(106, 153, 85)">手动</span> | 否 | 是 | 否 | 可读可写 | 最大长度32 |  |
| PushURLGeneratorServer | string | <span class="colour" style="color:rgb(106, 153, 85)">推流地址的服务器URL</span> | 否 | 是 | 否 | 可读可写 | 最大长度128 |  ||

##### 请求URL

 - `/RTMP/BasicConfig` 

##### 请求方式

- get
 
##### 成功返回示例  
```
[]

```

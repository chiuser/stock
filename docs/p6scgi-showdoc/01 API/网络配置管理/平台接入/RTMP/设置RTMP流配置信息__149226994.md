<!--
source: P6SCGI ShowDoc
item_name: P6SCGI API（CN）
item_id: 663863654
page_id: 149226994
source_url: http://p6scgi-api.p6sai.com:4999/web/#/663863654/149226994
catalog_path: 01 API / 网络配置管理 / 平台接入 / RTMP
-->

# 设置RTMP流配置信息
##### 简要描述

- ##### 说明

URI中的<ChannelID>显示通道编号，<ChannelID>从1开始。
<StreamID>表示流类型，通常“1”表示主流，“2”表示“第一子流”，“3”表示“第二子流”，依此类推。
当BasicConfig的<Mode>等于“manual”时，<Enable>字段和<PushURL>有效。 “true”使设备将流推送到RTMP服务器，“false”禁用RTMP服务。
<PushURL>字段是用于按RTMP协议推送流的RTMP URL。 当设备在手动模式下工作时，该字段必须由用户填写。
当设备在动态模式下工作时，<PlayURL>字段有效。 用户云通过Web浏览器打开此URL以观看流（ChannelID / StreamID）。

##### 请求

**xml**

```
<?xml version="1.0" encoding="utf-8"?>
<Config>
    <Enable><!--rw,req,xs:boolean;"false","true"--></Enable>
    <PushURL><!--rw,req,xs:string, the max length : 512--></PushURL>
    <PushURL_Base64><!--rw,req,xs:string, the max length : 512--></PushURL_Base64>
    <AudioEnable><!--rw,req,xs:boolean; "false","true"--></AudioEnable>
</Config>
```

**字段说明**

| 字段 | 类型 | 描述 | 是否必须 | 是否有效 | 是否可为空 | 可读可写(rw)/只读(ro)/只写(wo) | 取值范围/长度 | 备注 | 在APP的哪一个页面的哪一个字段 | 在web的哪一个页面的哪一个字段 | 字段受哪一个能力限制 | 字段受哪个字段限制 |
| --- | --- | --- | ---- | ---- | ----- | ---------------------- | ------- | --- | ---------------- | ---------------- | ---------- | --------- |
| Config |  |  |  |  |  |  |  |  |  |  |  | <span class="colour" style="color: rgb(206, 145, 120);">/System/DeviceInfo中的DeviceInfo-RTMP-ActiveStatus</span> |
| Enable | boolean | <span class="colour" style="color:rgb(106, 153, 85)">是否使能；false-否，true-是</span> | 是 | 是 | 否 | 可读可写 | false/true |  |  | <span class="colour" style="color:rgb(206, 145, 120)">配置-网络-RTMP设置-RTMP</span> | <span class="colour" style="color: rgb(85, 85, 85);">无</span> |  |
| PushURL | string | <span class="colour" style="color:rgb(106, 153, 85)">推流地址</span> | 是 | 是 | 否 | 可读可写 | 最大长度512 |  |  | <span class="colour" style="color:rgb(206, 145, 120)">配置-网络-RTMP设置-服务器地址</span> | <span class="colour" style="color: rgb(85, 85, 85);">无</span> | <span class="colour" style="color: rgb(85, 85, 85);">Enable</span> |
| PushURL_Base64 | string | <span class="colour" style="color:rgb(106, 153, 85)">推流地址base64编码</span> | 是 | 是 | 否 | 可读可写 | 最大长度512 |  |  | <span class="colour" style="color: rgb(85, 85, 85);">web内部计算</span> | <span class="colour" style="color: rgb(85, 85, 85);">无</span> | PushURL |
| AudioEnable | boolean | <span class="colour" style="color:rgb(106, 153, 85)">是否推音频；false-否，true-是</span> | 是 | 是 | 否 | 可读可写 | false/true |  |  | web没有适配 | <span class="colour" style="color: rgb(85, 85, 85);">无</span> | <span class="colour" style="color: rgb(85, 85, 85);">无</span> |

##### 响应

**xml**
无
**字段说明**
无

##### 请求URL

 - `/RTMP/ChannelID/StreamID/Config` 

##### 请求方式

- put
 
##### 成功返回示例  
```
{}

```

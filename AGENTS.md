# AGENTS

## 开发流程强约束

本仓库后续所有开发、重构、删除、迁移和部署相关改动，都必须遵守以下流程：

1. 先写技术方案：在动代码前，必须先说明目标、范围、影响面、风险点、回滚方式和验证方式。
2. 再梳理 LLD 文档：技术方案确认后，必须先创建或更新对应的 LLD 文档，把模块边界、接口、配置、数据流、异常处理和部署步骤写清楚。
3. 最后再开发：只有技术方案和 LLD 都明确后，才能进行代码实现、文件删除、脚本调整或部署配置修改。

禁止跳过技术方案或 LLD 直接改代码。即使是看起来简单的清理或删除，也要先确认方案和 LLD 边界，避免误删、误改或引入隐性架构债。

## 执行要求

- 如果用户直接要求开发，但尚未有技术方案和 LLD，需要先暂停编码，补齐方案与 LLD。
- 如果已有 LLD，需要先阅读并按 LLD 执行，不能做计划外改动。
- 如果实现过程中发现 LLD 与代码现状不一致，需要先反馈差异并更新 LLD，再继续实现。
- 提交代码前需要说明实际改动是否完全符合技术方案和 LLD。
- 本地真实配置统一写入 `.env.local`；不要新增 `.env.local2`、`.env.dev` 等临时本地配置文件。`.env.example` 只保留占位说明，禁止写入真实密码、Token、Owner 或其他密钥。
